type Query {
    allBooks: [Book]!
    findOne(id: ID): Book
}

type Book {
    id: ID!
    title: String
    pages: Int
    author: Author
}

type Author {
    id: ID!
    firstName: String
    lastName: String
    books: [Book]
}